# Mail
Deploy app locally:

source virtualenv/Scripts/activate

pip install -r requirements.txt

python manage.py runserver

For pythonanywhere:

Change in /project4/settings.py the "load_dotenv" configuration
pull from the console the new changes
